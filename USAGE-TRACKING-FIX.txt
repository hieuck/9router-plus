╔═══════════════════════════════════════════════════════════════════════════╗
║          ✅ FIX - Usage Tracking Accuracy Issue                          ║
╚═══════════════════════════════════════════════════════════════════════════╝

🐛 VẤN ĐỀ

Bạn báo: "theo dõi hạn mức đang không chính xác"

🔍 NGUYÊN NHÂN

Logic CŨ (SAI):
`csharp
// CHỈ dùng database KHI API không có usageCount VÀ limitCount
if (!usageCount.HasValue && !limitCount.HasValue && usageByConnection.TryGetValue(id, out var dbUsage))
{
    usageCount = dbUsage.Requests;  // Fallback
}
`

Vấn đề:
- API response có thể trả về usageCount/limitCount CŨ hoặc SAI
- Khi API có data (dù sai), code BỎ QUA database
- Database local là nguồn CHÍNH XÁC NHẤT (real-time từ 9Router)

═══════════════════════════════════════════════════════════════════════════

✅ GIẢI PHÁP

Logic MỚI (ĐÚNG):
`csharp
// LUÔN ưu tiên database cho usageCount (chính xác nhất)
if (usageByConnection.TryGetValue(id, out var dbUsage))
{
    // Override API usageCount với giá trị database (real-time, accurate)
    usageCount = dbUsage.Requests;
    
    // API limitCount được giữ nguyên (database không có limit info)
}
`

Ưu điểm:
✅ Database local LUÔN được ưu tiên cho usageCount
✅ API limitCount vẫn được dùng nếu có (database không track limit)
✅ Data hiển thị chính xác real-time từ 9Router SQLite

═══════════════════════════════════════════════════════════════════════════

📊 DATABASE VERIFICATION

Database path: %APPDATA%\9router\db\data.sqlite
Table: usageDaily (dateKey = "2026-08-22")

Sample data verified:
- 13f7aa39: 129 requests (Codex) ✅
- 2e5544c0: 105 requests (Kiro) ✅
- 85d8ddac: 119 requests (Kiro) ✅
- ee3eb532: 3 requests (Ollama) ✅

Total: 872 requests hôm nay

═══════════════════════════════════════════════════════════════════════════

📝 FILES CHANGED

1. src/RouterPlus.Infrastructure/Router/RouterApiClient.cs (+8 -4)
   - Changed database from fallback to PRIMARY source
   - Always override API usageCount with database value
   - Keep API limitCount (database doesn't have it)

2. tests/RouterPlus.Core.Tests/UsageInferenceIntegrationTests.cs (+2 -1)
   - Fixed test to pass usageByConnection parameter
   - Added using System.Collections.Generic

═══════════════════════════════════════════════════════════════════════════

✅ TESTS

Usage Inference Tests: 17/17 PASS ✅
- ParseConnection correctly uses database
- Inference fallback still works
- No regressions

═══════════════════════════════════════════════════════════════════════════

🎯 BEFORE vs AFTER

BEFORE (SAI):
- API response: usageCount = 50 (cũ)
- Database: requests = 129 (chính xác)
- Hiển thị: 50 requests ❌ SAI!

AFTER (ĐÚNG):
- API response: usageCount = 50 (bị ignore)
- Database: requests = 129 (chính xác)
- Hiển thị: 129 requests ✅ ĐÚNG!

═══════════════════════════════════════════════════════════════════════════

📋 PRIORITY CHAIN

1. Database (PRIMARY) - Real-time, local, accurate ⭐
2. API response (SECONDARY) - Có thể cũ/chậm
3. Inference from errors (FALLBACK) - Ước tính từ error messages

Usage Count: Database > Inference > null
Limit Count: API > Inference > null (database không có)
Reset Time: expiresAt > Database date > Inference > null

═══════════════════════════════════════════════════════════════════════════

✅ SUMMARY

Issue: Usage tracking không chính xác
Root cause: API response được ưu tiên hơn database local
Fixed: Đổi database thành PRIMARY source cho usageCount
Status: Fixed & Tested ✅

Database local bây giờ LUÔN được dùng cho usage tracking!

Time: 2026-08-22 20:28 UTC+7
Duration: ~30 phút

🎉 DONE!

╚═══════════════════════════════════════════════════════════════════════════╝
